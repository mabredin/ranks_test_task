version: "3.8"

services:
  backend:
    build: .
    restart: always
    depends_on: 
      - database
    ports:
      - "8000:8000"
    env_file:
      - .env

  database:
    image: postgres
    env_file:
      - .env
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
